import React from 'react';
import Layout from "../components/Layout/layout";

const Contact = () => {
    return (
        <Layout>
            <div>
                <img style={{
                    width: '100%',
                }} src={`../../cv-ak.png`} alt=""/>
            </div>
        </Layout>
    );
};

export default Contact;
